import Scene from '../components/scene/Scene'
import HUD from '../components/ui/HUD'

const CosmosNavigator = () => {
  return (
    <section className="page fullscreen-canvas">
      <Scene />
      <HUD />
    </section>
  )
}

export default CosmosNavigator
